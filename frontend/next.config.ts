import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 如果需要，可以重新启用类型检查和 ESLint
  // eslint: {
  //   ignoreDuringBuilds: false,
  // },
  // typescript: {
  //   ignoreBuildErrors: false,
  // },
};

export default nextConfig;